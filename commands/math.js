module.exports = {
	name: 'math',
	usage: '[math]',
	secret: true,
	description: 'do math',
	execute(message, args) {
		let input = '';
		for (let loop = 0; loop < args.length; loop++) { input += args[loop]; }
		console.log(imput);
		for (let loop = 0; loop < imput.length; loop++) {
            
        }
	},
};
